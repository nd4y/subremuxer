"""Sub Remuxer — a filtering proxy for proxy-subscription links."""

__version__ = "0.1.0"

#: How the app names itself to people. Identifiers — the package, the database
#: file, the session cookie, the export document kinds — deliberately stay
#: `subremuxer`, since renaming those would break existing installs.
APP_NAME = "Sub Remuxer"
